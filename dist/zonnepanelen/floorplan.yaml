type: picture-elements
image: /local/floorplan/transparent.png?version=BD6A55A068A92D35612D6890680BD29D
elements:
  - type: conditional
    conditions:
      []
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/base.png?version=19CD98D56E9DB0BBAAA8A502896D3E7B
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.tv
        state: 'on'
      - condition: state
        entity: light.erker_spots
        state: 'off'
      - condition: state
        entity: light.haard
        state: 'off'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.tv.png?version=8460D489872F72E1EB3B97A6D722F8BC
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.tv
        state: 'on'
      - condition: state
        entity: light.erker_spots
        state: 'on'
      - condition: state
        entity: light.haard
        state: 'off'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.tv_light.erker_spots.png?version=573F0FFFD54938E0A21A1D7558C83DD3
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.tv
        state: 'on'
      - condition: state
        entity: light.erker_spots
        state: 'on'
      - condition: state
        entity: light.haard
        state: 'on'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.tv_light.erker_spots_light.haard.png?version=B5DEF047AA97FA218D3304768DFC33F2
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.tv
        state: 'on'
      - condition: state
        entity: light.erker_spots
        state: 'off'
      - condition: state
        entity: light.haard
        state: 'on'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.tv_light.haard.png?version=4603A609217C8EE060D31C35459E8715
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.erker_spots'].state
          COLOR_MODE: states['light.erker_spots'].attributes.color_mode
          LIGHT_COLOR: states['light.erker_spots'].attributes.hs_color
          BRIGHTNESS: states['light.erker_spots'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.erker_spots
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.erker_spots.png?version=8460D489872F72E1EB3B97A6D722F8BC' :
              '/local/floorplan/light.erker_spots.red.png?version=850B2DF81CBD25BA5DD68243B0BAE0F8' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.erker_spots'].state
          COLOR_MODE: states['light.erker_spots'].attributes.color_mode
          LIGHT_COLOR: states['light.erker_spots'].attributes.hs_color
          BRIGHTNESS: states['light.erker_spots'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.erker_spots
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.erker_spots_light.haard.png?version=4603A609217C8EE060D31C35459E8715' :
              '/local/floorplan/light.erker_spots_light.haard.red.png?version=B6DFF062B189CB08DBF37B3D5BA34E84' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.tv
        state: 'off'
      - condition: state
        entity: light.erker_spots
        state: 'off'
      - condition: state
        entity: light.haard
        state: 'on'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.haard.png?version=FDB74516B078D8FCE52884FB72CBF0D3
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.keukentafel_spot
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.keukentafel_spot'].state
          COLOR_MODE: states['light.keukentafel_spot'].attributes.color_mode
          LIGHT_COLOR: states['light.keukentafel_spot'].attributes.hs_color
          BRIGHTNESS: states['light.keukentafel_spot'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.keukentafel_spot
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.keukentafel_spot.png?version=7A6B2F3EB5D53377F8F24BB4D94E17DC' :
              '/local/floorplan/light.keukentafel_spot.red.png?version=7334CFC54E5D038341363E81A73023C1' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.keukentafel_spot
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.keukentafel_spot'].state
          COLOR_MODE: states['light.keukentafel_spot'].attributes.color_mode
          LIGHT_COLOR: states['light.keukentafel_spot'].attributes.hs_color
          BRIGHTNESS: states['light.keukentafel_spot'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.keukentafel_spot
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.keukentafel_spot_light.aanrecht.png?version=319DA3867E0B416818118E9CE45F23BD' :
              '/local/floorplan/light.keukentafel_spot_light.aanrecht.red.png?version=0BB9FD5F4A63290220A76B0CCDA2FA56' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.keukentafel_spot
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.keukentafel_spot'].state
          COLOR_MODE: states['light.keukentafel_spot'].attributes.color_mode
          LIGHT_COLOR: states['light.keukentafel_spot'].attributes.hs_color
          BRIGHTNESS: states['light.keukentafel_spot'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.keukentafel_spot
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.keukentafel_spot_light.aanrecht_light.afzuigkap.png?version=139E18A3CD975DBED89D9E2A9661E4F0' :
              '/local/floorplan/light.keukentafel_spot_light.aanrecht_light.afzuigkap.red.png?version=D65D1ED375E6E7B4B00466C8325F2EAF' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.keukentafel_spot
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.keukentafel_spot'].state
          COLOR_MODE: states['light.keukentafel_spot'].attributes.color_mode
          LIGHT_COLOR: states['light.keukentafel_spot'].attributes.hs_color
          BRIGHTNESS: states['light.keukentafel_spot'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.keukentafel_spot
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.keukentafel_spot_light.afzuigkap.png?version=28090C620D12150C4F10C40BF2A013AD' :
              '/local/floorplan/light.keukentafel_spot_light.afzuigkap.red.png?version=DE60777E45F1F083E651ED46F211F84B' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.aanrecht
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.aanrecht'].state
          COLOR_MODE: states['light.aanrecht'].attributes.color_mode
          LIGHT_COLOR: states['light.aanrecht'].attributes.hs_color
          BRIGHTNESS: states['light.aanrecht'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.aanrecht
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.aanrecht.png?version=44F353422CC8844FACEFF2608B1DD115' :
              '/local/floorplan/light.aanrecht.red.png?version=BE0CDFDB6BAB4BDAF86993AF45AEDD5F' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.aanrecht
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.aanrecht'].state
          COLOR_MODE: states['light.aanrecht'].attributes.color_mode
          LIGHT_COLOR: states['light.aanrecht'].attributes.hs_color
          BRIGHTNESS: states['light.aanrecht'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.aanrecht
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.aanrecht_light.afzuigkap.png?version=3E41D2A29DD33F61E0A05C3A7601E907' :
              '/local/floorplan/light.aanrecht_light.afzuigkap.red.png?version=76ADD87BA8DA4941373649C808FF719A' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.keukentafel_spot
        state: 'off'
      - condition: state
        entity: light.aanrecht
        state: 'off'
      - condition: state
        entity: light.afzuigkap
        state: 'on'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.afzuigkap.png?version=A2EC990EA577106DA6801D601CEEF625
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.dimmer_overkapping
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.dimmer_overkapping'].state
          COLOR_MODE: states['light.dimmer_overkapping'].attributes.color_mode
          LIGHT_COLOR: states['light.dimmer_overkapping'].attributes.hs_color
          BRIGHTNESS: states['light.dimmer_overkapping'].attributes.brightness
          isInColoredMode: colorMode => ['hs', 'rgb', 'rgbw', 'rgbww', 'white', 'xy'].includes(colorMode)
        entities:
          - light.dimmer_overkapping
        element:
          type: image
          image: >-
              ${!isInColoredMode(COLOR_MODE) || (isInColoredMode(COLOR_MODE) && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.dimmer_overkapping.png?version=B983D317AFF45384ADB42A5EB20B16D3' :
              '/local/floorplan/light.dimmer_overkapping.red.png?version=E7B63F25F8D4073F43A69C41265D4144' }
        style:
          filter: '${ "hue-rotate(" + (isInColoredMode(COLOR_MODE) && LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
