type: picture-elements
image: /local/floorplan/transparent.png?version=9C24125EA37834676492FD53FB36ABC5
elements:
  - type: conditional
    conditions:
      []
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/base.png?version=3377252A3411A74F81A7654D40F2058C
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.erker_spots'].state
          COLOR_MODE: states['light.erker_spots'].attributes.color_mode
          LIGHT_COLOR: states['light.erker_spots'].attributes.hs_color
          BRIGHTNESS: states['light.erker_spots'].attributes.brightness
        entities:
          - light.erker_spots
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.erker_spots.png?version=7D3811253EB720EF3F3E4623E7B6E23D' :
              '/local/floorplan/light.erker_spots.red.png?version=286A48A8CBB3FE1ECFB1C162BD0471F5' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.erker_spots'].state
          COLOR_MODE: states['light.erker_spots'].attributes.color_mode
          LIGHT_COLOR: states['light.erker_spots'].attributes.hs_color
          BRIGHTNESS: states['light.erker_spots'].attributes.brightness
        entities:
          - light.erker_spots
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.erker_spots_light.haard.png?version=F8C85FA2AAAF8BD6FCBBFFC0936329F3' :
              '/local/floorplan/light.erker_spots_light.haard.red.png?version=500B8B748C0D6D4708ACB81558C7D3A1' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.erker_spots'].state
          COLOR_MODE: states['light.erker_spots'].attributes.color_mode
          LIGHT_COLOR: states['light.erker_spots'].attributes.hs_color
          BRIGHTNESS: states['light.erker_spots'].attributes.brightness
        entities:
          - light.erker_spots
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.erker_spots_light.haard_light.tv.png?version=98CE209B3034ADF8A7E61D1F622BE8A0' :
              '/local/floorplan/light.erker_spots_light.haard_light.tv.red.png?version=5CFBFD3CA26A41ED9F0EBB5992099A26' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.erker_spots'].state
          COLOR_MODE: states['light.erker_spots'].attributes.color_mode
          LIGHT_COLOR: states['light.erker_spots'].attributes.hs_color
          BRIGHTNESS: states['light.erker_spots'].attributes.brightness
        entities:
          - light.erker_spots
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.erker_spots_light.tv.png?version=5F1C845AB0F3D889A1D28D5A702F0039' :
              '/local/floorplan/light.erker_spots_light.tv.red.png?version=5304D65E1BC0F3C71085C5B6FE791F9C' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'off'
      - condition: state
        entity: light.haard
        state: 'on'
      - condition: state
        entity: light.tv
        state: 'off'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.haard.png?version=B89C39C4219CD4FEB7C52AC6688A062B
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'off'
      - condition: state
        entity: light.haard
        state: 'on'
      - condition: state
        entity: light.tv
        state: 'on'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.haard_light.tv.png?version=087953CFC9710F86AA0575440C1521A0
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.erker_spots
        state: 'off'
      - condition: state
        entity: light.haard
        state: 'off'
      - condition: state
        entity: light.tv
        state: 'on'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.tv.png?version=4AFCF289112046AC07A10720D105B246
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.aanrecht
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.aanrecht'].state
          COLOR_MODE: states['light.aanrecht'].attributes.color_mode
          LIGHT_COLOR: states['light.aanrecht'].attributes.hs_color
          BRIGHTNESS: states['light.aanrecht'].attributes.brightness
        entities:
          - light.aanrecht
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.aanrecht.png?version=98D868B607176E56C6E94F0101B17C93' :
              '/local/floorplan/light.aanrecht.red.png?version=D0BAEB3A3AA6A63A65314755EE97490F' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.aanrecht
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.aanrecht'].state
          COLOR_MODE: states['light.aanrecht'].attributes.color_mode
          LIGHT_COLOR: states['light.aanrecht'].attributes.hs_color
          BRIGHTNESS: states['light.aanrecht'].attributes.brightness
        entities:
          - light.aanrecht
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.aanrecht_light.keukentafel_spot.png?version=9673BBC0D3819698C455BEA3F0372067' :
              '/local/floorplan/light.aanrecht_light.keukentafel_spot.red.png?version=1BE4E42DB5295563C5A37A9AD8301C42' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.aanrecht
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.aanrecht'].state
          COLOR_MODE: states['light.aanrecht'].attributes.color_mode
          LIGHT_COLOR: states['light.aanrecht'].attributes.hs_color
          BRIGHTNESS: states['light.aanrecht'].attributes.brightness
        entities:
          - light.aanrecht
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.aanrecht_light.keukentafel_spot_light.afzuigkap.png?version=8C748523007612C8B65F23A20B62059C' :
              '/local/floorplan/light.aanrecht_light.keukentafel_spot_light.afzuigkap.red.png?version=7AC4AAA7AFAC156FBDACD854482B947E' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.aanrecht
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.aanrecht'].state
          COLOR_MODE: states['light.aanrecht'].attributes.color_mode
          LIGHT_COLOR: states['light.aanrecht'].attributes.hs_color
          BRIGHTNESS: states['light.aanrecht'].attributes.brightness
        entities:
          - light.aanrecht
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.aanrecht_light.afzuigkap.png?version=0413F75D4D9839CF69C02B76C050AB11' :
              '/local/floorplan/light.aanrecht_light.afzuigkap.red.png?version=01BECAE711D4B0819855AF4EF3E98C00' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.keukentafel_spot
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.keukentafel_spot'].state
          COLOR_MODE: states['light.keukentafel_spot'].attributes.color_mode
          LIGHT_COLOR: states['light.keukentafel_spot'].attributes.hs_color
          BRIGHTNESS: states['light.keukentafel_spot'].attributes.brightness
        entities:
          - light.keukentafel_spot
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.keukentafel_spot.png?version=0829AC9668F5BEA71184269351991E22' :
              '/local/floorplan/light.keukentafel_spot.red.png?version=181FD95D2006BF8B6C441C58930A6712' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.keukentafel_spot
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.keukentafel_spot'].state
          COLOR_MODE: states['light.keukentafel_spot'].attributes.color_mode
          LIGHT_COLOR: states['light.keukentafel_spot'].attributes.hs_color
          BRIGHTNESS: states['light.keukentafel_spot'].attributes.brightness
        entities:
          - light.keukentafel_spot
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.keukentafel_spot_light.afzuigkap.png?version=70860648BB3558905CEC8EF8EC0E31FE' :
              '/local/floorplan/light.keukentafel_spot_light.afzuigkap.red.png?version=BD1124E4E0EB551083900837C07805A7' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.aanrecht
        state: 'off'
      - condition: state
        entity: light.keukentafel_spot
        state: 'off'
      - condition: state
        entity: light.afzuigkap
        state: 'on'
    elements:
      - type: image
        tap_action:
          action: none
        hold_action:
          action: none
        image: /local/floorplan/light.afzuigkap.png?version=3C3945E8032B5B4A40D99D92A27B6E0A
        filter: none
        style:
          left: 50%
          top: 50%
          width: 100%
  - type: conditional
    conditions:
      - condition: state
        entity: light.dimmer_overkapping
        state: 'on'
    elements:
      - type: custom:config-template-card
        variables:
          LIGHT_STATE: states['light.dimmer_overkapping'].state
          COLOR_MODE: states['light.dimmer_overkapping'].attributes.color_mode
          LIGHT_COLOR: states['light.dimmer_overkapping'].attributes.hs_color
          BRIGHTNESS: states['light.dimmer_overkapping'].attributes.brightness
        entities:
          - light.dimmer_overkapping
        element:
          type: image
          image: >-
              ${COLOR_MODE === 'color_temp' || COLOR_MODE === 'brightness' || ((COLOR_MODE === 'rgb' || COLOR_MODE === 'hs') && LIGHT_COLOR && LIGHT_COLOR[0] == 0 && LIGHT_COLOR[1] == 0) ?
              '/local/floorplan/light.dimmer_overkapping.png?version=23C90F4B9F3D0AB381803D917FB14E09' :
              '/local/floorplan/light.dimmer_overkapping.red.png?version=9522FBA58BDC3EA37A4E2A7BDB577686' }
        style:
          filter: '${ "hue-rotate(" + (LIGHT_COLOR ? LIGHT_COLOR[0] : 0) + "deg)"}'
          opacity: '${LIGHT_STATE === ''on'' ? (BRIGHTNESS / 255) : ''100''}'
          mix-blend-mode: lighten
          pointer-events: none
          left: 50%
          top: 50%
          width: 100%
  - type: state-icon
    entity: light.erker_spots
    title: null
    style:
      top: 55.73%
      left: 48.74%
      border-radius: 50%
      text-align: center
      background-color: rgba(255, 255, 255, 0.3)
      opacity: 100%
    tap_action:
      action: toggle
    double_tap_action:
      action: none
    hold_action:
      action: more-info
  - type: state-icon
    entity: light.tv
    title: null
    style:
      top: 56.26%
      left: 54.05%
      border-radius: 50%
      text-align: center
      background-color: rgba(255, 255, 255, 0.3)
      opacity: 100%
    tap_action:
      action: toggle
    double_tap_action:
      action: none
    hold_action:
      action: more-info
  - type: state-label
    entity: sensor.woonkamer_temperature
    title: null
    style:
      top: 51.05%
      left: 53.65%
      border-radius: 50%
      text-align: center
      background-color: rgba(255, 255, 255, 0.3)
      opacity: 100%
    tap_action:
      action: more-info
    double_tap_action:
      action: none
    hold_action:
      action: more-info
  - type: state-icon
    entity: camera.carport
    title: null
    style:
      top: 48.73%
      left: 38.88%
      border-radius: 50%
      text-align: center
      background-color: rgba(255, 255, 255, 0.3)
      opacity: 100%
    tap_action:
      action: more-info
    double_tap_action:
      action: none
    hold_action:
      action: more-info
  - type: state-icon
    entity: binary_sensor.multisensor_overkapping_motion_detection_2
    title: null
    style:
      top: 50.88%
      left: 47.19%
      border-radius: 50%
      text-align: center
      background-color: rgba(255, 255, 255, 0.3)
      opacity: 100%
    tap_action:
      action: more-info
    double_tap_action:
      action: none
    hold_action:
      action: more-info
  - type: state-icon
    entity: camera.overkapping
    title: null
    style:
      top: 44.28%
      left: 60.16%
      border-radius: 50%
      text-align: center
      background-color: rgba(255, 255, 255, 0.3)
      opacity: 100%
    tap_action:
      action: more-info
    double_tap_action:
      action: none
    hold_action:
      action: more-info
